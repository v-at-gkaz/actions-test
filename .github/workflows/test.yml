name: CI

on:
  push:
    branches: ["main"]

jobs:
  clone:
    runs-on: alt-runner

    steps:
    - uses: actions/checkout@v2
    - name: Get Last Code
      run: git clone --single-branch --branch main https://github.com/v-at-gkaz/js-sandbox-2024

  build:
    needs: clone
    runs-on: alt-runner

    steps:
    - uses: actions/checkout@v2
    - name: Get Last Code
      run: cd $GITHUB_WORKSPACE/js-sandbox-2024/project_18_spa && npm ci && npm run build
